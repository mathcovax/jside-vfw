import express from "express"

/**
 * 
 * @param {express.Request} req
 * @param {express.Response} res
 * @param {(req: express.Request, res: express.Response) => Boolean} httpAcces
 * @return {Boolean}
 */
export default function(req, res, httpAcces){
    return true
}